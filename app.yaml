name: text-to-video
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/text-to-video
  original_url: https://huggingface.co/spaces/Chris4K/text-to-video

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
